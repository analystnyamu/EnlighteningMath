[build]
  command = "curl -sSL https://github.com/quarto-dev/quarto-cli/releases/latest/download/quarto-linux-amd64 -o quarto && chmod +x quarto && mv quarto /usr/local/bin/quarto && quarto render || { echo 'Quarto render failed'; exit 1; }"
  publish = "_book"
