[build]
  command = "curl -sSL https://github.com/quarto-dev/quarto-cli/releases/latest/download/quarto-linux-amd64.deb -o quarto.deb && dpkg -i quarto.deb || { echo 'Quarto installation failed'; exit 1; } && quarto render || { echo 'Quarto render failed'; exit 1; }"
  publish = "_book"
