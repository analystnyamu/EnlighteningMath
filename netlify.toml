[build]
  publish = "_site"  # or "_book" depending on your Quarto output directory
  command = "wget https://github.com/quarto-dev/quarto-cli/releases/download/v1.3.340/quarto-1.3.340-linux-amd64.deb && sudo dpkg -i quarto-1.3.340-linux-amd64.deb && quarto render"

[build.environment]
  NODE_VERSION = "16"  # Optional, if needed
